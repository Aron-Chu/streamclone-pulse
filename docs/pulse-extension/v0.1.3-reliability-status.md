# v0.1.3 reliability — local acceptance status

Updated: 2026-07-26 (local acceptance closure pass)

## Heads (unchanged bases; WIP only)

| Repo | Branch | HEAD |
|------|--------|------|
| Pulse | feat/v013-reliability | `a4330ca0eea733d60f307ce5cb4e6e34612f5797` |
| Backend | feat/v013-reliability | `0a4f31610d171165d876169f1b30f571cdeb1c0c` |

Protected dirty checkouts untouched. Recovery snapshots are local-only and are not documented with machine paths here.

## Control matrix (fixtures)

- A offline/stale: xqc / `320567744986` — Twitch offline, Pulse still live, 7 games id=0, coverage start 153
- B healthy live: nmplol / `319549121886` — both live, 1 game id=0, coverage start **377** (this capture), 30d emotes partial/lowConfidence empty

Canary (read-only): `scripts/v013-reliability-canary.ps1`.

## Batch status (candidate acceptance **pending**)

| Batch | Status |
|-------|--------|
| 0 Cross-channel harness | Local fixtures + canary |
| 1 Stale-live/rollover | Local unit proof; hosted activation pending migrate+flags |
| 2 Durable VOD worker | Code + flags default false; PG race suite needs `TEST_DATABASE_URL` |
| 3 Portal ended UX | Status merge + no `?t=` without verified align; remount E2E pending |
| 4 Games both controls | Deterministic IDs + fixture unit tests; browser snapshots pending |
| 5 Emote parity | providerId/key on minutes, live-tail `afterOffset`, catalog honesty |
| 6 Picker/7TV | Picker tests + ADR proposed only; EventAPI forced false; `cmd/emote` held |

## Migration decision

- Sequential `000071_vod_resolution_jobs` (not `100000`).
- Hosted activation requires explicit migrate + `PULSE_DURABLE_VOD_WORKER=true` + `PULSE_STALE_LIVE_RECONCILER=true` after schema readiness — **not** part of this local pass.

## Hard stop

No commit/push/deploy/migrate/tag/upload.
Hosted xQc remains stale-live until migrate + deploy + reconcile evidence.
Candidate acceptance: **pending**.

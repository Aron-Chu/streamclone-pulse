# v0.1.3 reliability — source acceptance status

Updated: 2026-07-27 (hardening Gate A source work)

## Truthfulness

- Target extension/portal version remains **0.1.3**.
- This document describes **source** readiness. It does **not** claim hosted migration,
  image publication, CWS/Edge upload, or store rollout.

## Migration

- Durable VOD jobs migration is **`100000_vod_resolution_jobs`** (backend-only band).
- Sequential `000071_*` is forbidden and was never deployed (source + private ops metadata;
  production schema inspection remains owner-gated).
- New analytics binaries fail `/readyz` when the durable schema is absent/malformed even
  with workers disabled.

## Runtime flags (defaults)

| Flag | Default |
|------|---------|
| `PULSE_DURABLE_VOD_WORKER` | false |
| `PULSE_STALE_LIVE_RECONCILER` | false |
| `PULSE_STALE_LIVE_DRY_RUN` | true when reconciler enabled |
| RPR-3 diagnostics / RPR-4 support / RPR-5 PostHog / 7TV EventAPI | disabled |

Stale mutation additionally requires a private allowlist and two independent offline
confirmations. Channel allowlists are never committed.

## Deferred (not in v0.1.3)

- 7TV EventAPI / `cmd/emote` runtime ownership
- Extension Sentry activation, PostHog activation
- Hosted support/Turnstile/Linear/email activation
- Firefox/AMO

## Hard stop

No production migrate/deploy/tag/upload without a later explicit owner authorization.

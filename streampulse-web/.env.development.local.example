# Copy to `.env.development.local` (gitignored) to persist a dev backend override.
# Not required — `npm run dev` defaults to https://api.streampulse.stream when absent.
#
# Vite dev server: http://127.0.0.1:5173
#
# Hosted production API (IRC + corpus on streampulse-vps):
# VITE_BACKEND_URL=https://api.streampulse.stream

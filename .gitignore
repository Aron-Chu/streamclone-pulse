node_modules/
dist/
*.zip
*.zip.sha256
.DS_Store

# Local env / secrets (never commit)
.env
.env.*
!.env.example
*.local
.env.local

# Playwright (uploaded by CI on failure; never commit)
test-results/
playwright-report/
playwright/.cache/
*.webm
**/trace.zip

_overlay*.tsx*
_split_overlay.*
.overlay-full.patch

# Local release / CWS prep artifacts (never commit)
.artifacts/
listing-assets/

# .vscode/ — commit shared config (settings.json, mcp.json),
# .vscode/.gitignore handles local-only auth / browser / log state.

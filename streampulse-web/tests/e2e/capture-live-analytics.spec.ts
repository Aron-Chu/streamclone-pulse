/**
 * @deprecated Use `npm run capture:analytics` (analytics-visual-capture.spec.ts).
 * Hosted-only captures at 1600 / 1024 / 390px against https://api.streampulse.stream.
 */
import { test } from '@playwright/test'

test.skip('legacy capture — run npm run capture:analytics instead', async () => {})

# Brand mark decision — Peak spike

**Chosen concept:** single chart peak spike (formerly review mockup `mark-peak-spike.png`).

## Shipped assets (runtime)

| Surface | Path |
|---------|------|
| Portal React mark | `streampulse-web/src/ui/components/BrandMark.tsx` |
| Portal SVG | `streampulse-web/public/brand-peak.svg` |
| Favicon | `streampulse-web/public/favicon.svg` |
| Extension mark | `src/ui/PeakBrandMark.tsx` |

Visual: hollow teal angular peak-spike (pulse wings + tall center peak) on a near-black rounded tile.
Runtime SVG geometry matches the chosen `mark-peak-spike.png` outline (not the earlier filled mountain + dot).

## Rejected concepts (not shipped)

- Refined 3-bar signal
- Soft pulse / waveform
- SP monogram on teal tile

PNG review mockups under `streampulse-web/docs/brand-mockups/` were deleted after this decision was recorded (2026-07-18). Recovery copies live outside the repo if needed.

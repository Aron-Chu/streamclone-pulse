node_modules/
dist/
*.zip
*.zip.sha256
streampulse-extension-*.validation.json
.DS_Store

# Local env / secrets (never commit)
.env
.env.*
!.env.example
*.local
.env.local

# Playwright (uploaded by CI on failure; never commit)
test-results/
playwright-report/
playwright/.cache/
*.webm
**/trace.zip

_overlay*.tsx*
_split_overlay.*
.overlay-full.patch

# Local release / CWS prep artifacts (never commit)
.artifacts/
listing-assets/

# Uncertified / operator-regenerated CWS capture outputs (not the approved mocked RC set)
docs/pulse-extension/cws-screenshots/

# Visual companion mockups (brainstorming)
.superpowers/

# Ad-hoc command logs written at the repo root (archive under .artifacts/)
/*.log
# Local agent plans and nested worktrees. They can hold private operator notes
# and copies of other branches; never publish them from this public repo.
/.claude/plans/
/.claude/worktrees/
/.worktrees/
